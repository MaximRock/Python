version: '3.7'

services:
  regression:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./docker-results/:/code/allure-results
      - ./screenshot/:/code/screenshot
    command: ["pytest", "-v", "-m all_tests", "tests/test_page.py", "--alluredir=allure-results"]

